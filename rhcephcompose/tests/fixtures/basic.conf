builds = {
    'trusty': 'builds-ceph-2.0-trusty.txt',
}

comps = {
    'trusty': 'comps-basic.xml',
}

variants_file = 'variants-basic.xml'

chacra_url = 'https://chacra.example.com/'

target = 'trees'

product_version = '2'

extra_files = [
    {'file': 'README'},
    {'file': 'EULA'},
    {'file': 'GPL'},
]
